GAME MECHANICS
    - random picks from word list 
        (I will create a list and use random function)
    - 6 attempts for the player
        (I will create a var which keeps track of the failed attempts and deletes it every time player has a wrong guess)
    - input is single letter per turn
    - win logic is to guess all the letters correctly before attempts end
        (I will always have to check the attempts list is and if attempts count goes to negative players loses)
    - lose when lives/attempts hit zero

input validation restrictions:
    - non-alphabet, multi-char, already gueesed letters reject
    - case sensitive

game state
    - secret_word
    - guessed_letters (set)
    - wrong_guesses (list)
    - lives_remaining (int, starts 6)
    - display_word (e.g. _ a _ _ m a n)